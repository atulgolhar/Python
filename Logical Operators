while True:
    line = input("small talk with user")
    if line:
        try:
            newnumber = int(line)
        except ValueError as err:
            print(err)
        except Exception2 as variable2:
            # do something
        except Exception3 as variable3:
            # do something    
        except Exception4 as variable4:
            # do something    
        increment data
    else:
        break
  
