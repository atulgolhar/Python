#!/usr/bin/env python3
# simpleLambda.py

# simple Lambda usage

>>> (lambda x: x + 1)(2)
3

>>> add_one = lambda x: x+1
>>> add_one(3)
4

>>> def add_oneFunction(x):
    return x + 1

>>> add_oneFunction(5)
6
